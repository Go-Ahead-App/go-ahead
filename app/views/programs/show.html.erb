<p style="color: green"><%= notice %></p>

<div class="flex justify-center mt-8 mb-4">
  <section class="w-3/4 flex justify-between items-center mb-4 z-10">
    <div>
      <%= link_to "Back to Programs", programs_path, class: "px-6 py-4 text-sm bg-neutral-100 text-neutral-700 hover:bg-neutral-50 duration-200 cursor-pointer rounded-md" %>
    </div>

    <% if admin? %>
      <div>
        <%= link_to "Edit Program", edit_program_path(@program), class: "px-6 py-4 text-sm bg-neutral-100 text-neutral-700 hover:bg-neutral-50 duration-200 cursor-pointer rounded-md" %>
      </div>
    <% end %>
  </section>
</div>

<%= render @program %>
