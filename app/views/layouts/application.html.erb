<!DOCTYPE html>
<html>
  <head>
    <title>GoAhead</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application" %>
    <%= javascript_include_tag "application", defer: true %>

    <%= vite_client_tag %>
    <%= vite_javascript_tag 'application', defer: 'defer' %>

    <script src="https://unpkg.com/phosphor-icons"></script>
  </head>

  <body class="bg-white text-neutral-900 dark:bg-black dark:text-neutral-100 <%= action_name == 'home' ? '' : 'pt-12 md:pt-16 xl:pt-24' %>">
    <%= render "shared/header" %>

    <main class="w-11/12 md:w-3/4 mx-auto min-h-screen">
      <%= yield %>
    </main>

    <%= render "shared/footer" %>
  </body>
</html>
