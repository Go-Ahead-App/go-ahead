<section>
  <%= form_for [@board, @note], html: { class: "lg:w-1/2 mx-auto md:mt-32" } do |f| %>
    <h1 class="font-display xl:text-7xl mb-8">Create a new note</h1>

    <% if flash[:form_errors].present? %>
      <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative mb-8" role="alert">
        <strong class="font-bold">Error!</strong>
        <span class="block sm:inline"><%= flash[:form_errors] %></span>
      </div>
    <% end %>

    <div class="flex flex-col">
      <%= f.hidden_field :user_id, value: current_user.id %>
      <%= f.hidden_field :board_id, value: @board.id %>

      <div class="mb-4">
        <%= f.label :title, class: "text-neutral-600" %>
        <%= f.text_field :title, class: "px-4 py-3 bg-transparent border border-neutral-500 w-full focus:outline-0 focus:border-neutral-100 duration-200 rounded-md" %>
      </div>

      <div class="mb-4">
        <%= f.label :description, class: "text-neutral-600" %>
        <%= f.text_area :description, class: "px-4 py-3 bg-transparent border border-neutral-500 w-full focus:outline-0 focus:border-neutral-100 duration-200 rounded-md" %>
      </div>

      <div class="mb-4">
        <%= f.label :content, class: "text-neutral-600" %>
        <%= f.text_area :content, class: "px-4 py-3 bg-transparent border border-neutral-500 w-full focus:outline-0 focus:border-neutral-100 duration-200 rounded-md" %>
      </div>


      <div class="mb-4">
        <%= f.submit "Create a note", class: "px-6 py-4 text-sm bg-neutral-100 text-neutral-700 hover:bg-neutral-50 duration-200 cursor-pointer rounded-md" %>
      </div>
    </div>
  <% end %>
</section>
